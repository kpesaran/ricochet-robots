import * as THREE from 'three' 

export type Textures = {
    wallARMTexture: THREE.Texture;
    wallColorTexture: THREE.Texture;
    wallTextureDisp: THREE.Texture;
    wallNormalTexture: THREE.Texture;
};

